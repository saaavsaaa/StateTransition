package date.iterator.state;

public class ChatNode {
    //指向原字符串，树生成完毕后销毁

    //在原字符串中位置

    // 节点字符，root节点值为'/'
    private char value = '/';

    // 上级节点

    //最长后缀

    //原字符串长度，int数组，只有当该字符为结尾字符时有值，否则是null
}
